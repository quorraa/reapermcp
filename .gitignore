/target
**/*.rs.bk
*.pdb

# Local runtime state (never committed)
/ipc/
*.log
.DS_Store
Thumbs.db

# MCP host configuration. The real file holds absolute paths for one machine
# and one user account; copy .mcp.json.example, or generate it with
# `qlabs-reaper-music-mcp print-mcp-config`.
/.mcp.json

# Packaging output
/dist/

# Python bytecode from the examples
__pycache__/
*.pyc

# throwaway projects written to clear REAPER's modified flag
examples/five-genres/discard/
